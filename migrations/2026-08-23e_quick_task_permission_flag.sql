-- ============================================================================
-- BSD-CRM — Quick Task Add: permission flag on profiles
-- תאריך: 23/08/2026
-- ============================================================================
-- דגל בוליאני חדש, לפי אותו דפוס בדיוק כמו can_create_businesses/can_record
-- הקיימים: ברירת מחדל false לכולם. אדמין (role='admin') תמיד מורשה בלי תלות
-- בדגל - זה נבדק בקוד (js/permissions.js + Edge Function), לא כאן.
--
-- הדגל הזה שולט אך ורק על הגישה למסך המהיר ולשירות התמלול/חילוץ שלו - הוא
-- לא נוגע ולא מגביל את יכולת יצירת המשימות הרגילה שכל משתמש כבר יש לו
-- דרך tasks.html (tasks_insert RLS, ללא שינוי).
-- ============================================================================

alter table profiles add column if not exists can_quick_add_task boolean default false;
